# Shell scripts must keep LF endings in the working tree.  The Linux artifacts
# are built from this same checkout via WSL, and `#!/usr/bin/env bash\r` is not
# an interpreter -- with CRLF, build-appimage.sh fails as "env: 'bash\r': No
# such file or directory".
*.sh text eol=lf

# PowerShell stays CRLF; Windows PowerShell 5.1 is happiest with it.
*.ps1 text eol=crlf

# The C++ parity corpus is written with LF by MDBossCpp/tests/make_golden.py.
# Checked out as CRLF it would differ from what that script produces, so every
# regeneration would show up as a whole-corpus diff and hide the real change.
MDBossCpp/tests/golden/** text eol=lf
